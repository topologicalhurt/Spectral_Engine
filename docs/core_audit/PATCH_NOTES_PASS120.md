# Core audit pass 120: master-plan closure criteria

Pass 120 adds explicit stop criteria for the long audit/refactor campaign.

The purpose is to prevent the project from drifting back into tiny guard passes. Future work must
fix a real bug, remove real duplication, reduce API surface, improve behavioral tests, or simplify
a real performance path.
