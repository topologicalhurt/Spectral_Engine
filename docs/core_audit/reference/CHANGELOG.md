# Core audit changelog

One line per audit pass. Full rationale/diffs are in git history
(the per-pass note files were consolidated here). Newer work uses terse
per-commit messages, not per-pass note files (see AI_CANON Rule 18).

## Campaign 1 — host-kernel audit (passes 1–137)

- Pass 1 — foundational math and fast-math cleanup
- Pass 2 — approximation gates and validation workflow
- Pass 3 — fused-frame pair contract
- Pass 4 — kernel API hardening
- Pass 5 — GPU tile-span canonicalization
- Pass 6 — FFT resource ownership hardening
- Pass 7 — FFT allocator boundary safety
- Pass 8 — STFT window registry and magnitude calibration
- Pass 9 — window metric validity flags
- Pass 10 — window-aware peak interpolation
- Pass 11 — peak estimator module
- Pass 12 — estimator contract hardening
- Pass 13 — estimator validation harness
- Pass 14 — sub-bin temporal slope for `df`
- Pass 15 — phase-advance diagnostics
- Pass 16 — phase-consistency policy
- Pass 17 — window-aware amplitude estimation
- Pass 18 — validated peak-model profile
- Pass 19 — estimator safety and audit bump
- Pass 20 — peak-model integration audit
- Pass 21 — kernel alias and wrapper slimming
- Pass 22 — synthesis output-size overflow contract
- Pass 23 — CPU synth thread-arena stride overflow
- Pass 24 — tracker segment-storage allocation overflow
- Pass 25 — tracker error propagation preserves root cause
- Pass 26 — segment cache index insertion overflow
- Pass 27 — segment cache append blob byte-count contract
- Pass 28 — segment cache index load count/file-size contract
- Pass 29 — segment cache lookup metadata and data extent contract
- Pass 30 — segment cache key and store scalar metadata contract
- Pass 31 — segment cache input identity contract
- Pass 32 — hash file full-direct size and capability contract
- Pass 33 — segment file metadata and shape contract
- Pass 34 — wavetable .spwt file shape and sample contract
- Pass 35 — wavetable raw/hex/buffer ingestion contract
- Pass 36 — audio file frame-count representability contract
- Pass 37 — audio file finite-sample contract
- Pass 38 — WAV PEAK scrubber chunk extent contract
- Pass 39 — synthesis derived-parameter finiteness contract
- Pass 40 — segment cache payload validity contract
- Pass 41 — segment loop derived-scalar contract
- Pass 42 — GPU synth parameter packing contract
- Pass 43 — CUDA event lifecycle contract
- Pass 44 — Metal dispatch completion contract
- Pass 45 — GPU tile fill cursor contract
- Pass 46 — CUDA async transfer and timing contract
- Pass 47 — segment loop sample-offset float representability contract
- Pass 48 — GPU tile preprocess segment-ID narrowing contract
- Pass 49 — zero-reference GPU tile dispatch contract
- Pass 50 — GPU tile cache validation contract
- Pass 51 — CPU synth reduction byte-count contract
- Pass 52 — float normalization finite-state contract
- Pass 53 — audio window time-domain contract
- Pass 54 — wavetable HEX record shape contract
- Pass 55 — hash file reset lifecycle contract
- Pass 56 — Q15 normalization CMSIS length contract
- Pass 57 — mono-to-stereo index-domain contract
- Pass 58 — WAV RIFF declared-extent contract
- Pass 59 — vector complex-interleaved index contract
- Pass 60 — vector primitive null/span contract
- Pass 61 — FFT frame dispatch range/thread contract
- Pass 62 — FFT magnitude scaling finite-output contract
- Pass 63 — hash consume-file lifecycle contract
- Pass 64 — hash oneshot null-span contract
- Pass 65 — window metric finite-input contract
- Pass 66 — fused-analysis scratch allocation byte-count contract
- Pass 67 — fused-analysis OpenMP resource-thread contract
- Pass 68 — fused-analysis frame-time float contract
- Pass 69 — FFT single-frame helper boundary contract
- Pass 70 — analysis OpenMP thread-count domain contract
- Pass 71 — tracker scalar-domain derivation contract
- Pass 72 — tracker process matrix and frame-time contract
- Pass 73 — tracker candidate-batch bounds contract
- Pass 74 — peak amplitude gain-bound finite-product contract
- Pass 75 — peak phase-advance finite-product contract
- Pass 76 — Jacobsen/Candan complex-offset finite-product contract
- Pass 77 — Quinn second estimator finite-product contract
- Pass 78 — magnitude-parabolic estimator finite-product contract
- Pass 79 — peak window callback neighborhood contract
- Pass 80 — fast sqrt finite-input contract
- Pass 81 — tracker emitted-segment validity contract
- Pass 82 — tracker stats accumulation overflow contract
- Pass 83 — tracker creation thread/frequency index-domain contract
- Pass 84 — tracker byte-estimate STFT accounting overflow contract
- Pass 85 — tracker candidate best-next integer-domain contract
- Pass 86 — synthesis segment payload preflight contract
- Pass 87 — GPU segment cache one-shot identity contract
- Pass 88 — GPU tile cache one-shot layout identity contract
- Pass 89 — oscillator width and quantized-cast contract
- Pass 90 — CPU synth finite-output postcondition
- Pass 91 — contract routing and alias-wrapper removal
- Pass 92 — synthesis preflight consolidation and legacy API removal
- Pass 93 — analysis window context consolidation
- Pass 94 — fused-analysis scratch row ownership
- Pass 95 — tracker peak-model mutation consolidation
- Pass 96 — shared OpenMP effective-thread contract
- Pass 97 — FFT frame dispatch consolidation
- Pass 98 — full-analysis STFT matrix ownership
- Pass 99 — tracker frame-time helper consolidation
- Pass 100 — peak offset clamp consolidation
- Pass 101 — tracker stats accumulation helper consolidation
- Pass 102 — peak clamp consolidation regression repair
- Pass 103 — peak magnitude triplet helper consolidation
- Pass 104 — analysis shape contract consolidation
- Pass 105 — checked accumulation contract promotion
- Pass 106 — architecture cleanup status and next-step handoff
- Pass 107 — prepared GPU dispatch plan
- Pass 108 — Metal prepared GPU dispatch wiring
- Pass 109 — CUDA prepared GPU dispatch wiring
- Pass 110 — GPU cache lookup encapsulation
- Pass 111 — Phase D completion status
- Pass 112 — tracker candidate batch owner
- Pass 113 — tracker frame-context constructor
- Pass 114 — tracker worker-stats context
- Pass 115 — tracker flush API encapsulation
- Pass 116 — Phase E candidate-context status
- Pass 117 — analysis path policy object
- Pass 118 — explicit analysis path-mode entry point
- Pass 119 — full/fused parity harness specification
- Pass 120 — master-plan closure criteria
- Pass 121 — Phase F seam completion status
- Pass 128 — Window generation status contract
- Pass 129 — Fixed-point finite narrowing contract
- Pass 130 — LUT lookup domain contract
- Pass 131 — ARM32 process/load bounds
- Pass 132 — ARM simulation preflight and conversion bounds
- Pass 133 — ARM32 chirp contract
- Pass 135 — ARM32 load invariants
- Pass 136 — ARM32 DMA placement/coherency contract
- Pass 137 — ARM simulation unsigned phase parity

## Campaign 2 — ARM / contract / CTF / harness (passes 138+)

- Pass 138 — fix self-referential analysis path-decision struct
- Pass 139 — interim ARM-synth behavior oracle
- Pass 140 — remove hand-written ARM NEON from spectral_q15.c
- Pass 141 — ARM A1 cleanups (LUT-reader dedup, uq32_t); freq-units verified
- Pass 142 — make the M7 codepath host-buildable (ARM verification enabler)
- Pass 143 — CTest correctness harness over the REAL ARM process
- Pass 144 — fix ARM frequency scaling (rendered ~sr/2pi too low)
- Pass 145 — fix ARM output amplitude (Q30 accumulator read as Q31, -6 dB)
- Pass 146 — wire load-path segment validation; add rejection test
- Pass 147 — device-agnostic memory-class placement (Phase E, increment 1)
- Pass 148 — build-select desktop-only core modules (Phase E)
- Pass 149 — remove redundant host/sim whole-file guards (Phase E)
- Pass 150 — fs.c — drop dead embedded stub branch + guard (Phase E)
- Pass 151 — wavetable.c device-agnostic sample abstraction (Phase E)
- Pass 152 — oscillator_simd.c port-layer split (Phase E)
- Pass 153 — spectral_vector_ops.c port-layer relocation (Phase E)
- Pass 154 — spectral_out.c kernel port-layer split (Phase E)
- Pass 155 — spectral_synth_internal.c GPU-tile port-layer split (Phase E)
- Pass 157 — sim becomes a perf model over the REAL spectral_arm32_process (Phase A / A1b#3)
- Pass 158 — retire the interim ARM oracle (Phase A / A1b#4 closure)
- Pass 159 — COLA/WOLA reconstruction invariant + test (Phase B0)
- Pass 160 — guarantee manifest + self-report API (Phase B1+B2)
- Pass 161 — CTF sweep increment 1 — fixed-point UB cluster (Phase C)
- Pass 162 — CTF sweep increment 2 — analysis/peak-track tracker cluster (Phase C)
- Pass 163 — CTF sweep increment 3 — port/SIMD/out cluster (Phase C)
- Pass 164 — CTF sweep increment 4 — hashing/parsing/path cluster (Phase C)
- Pass 165 — CTF sweep increment 5 — DSP-math / FFT-scaling cluster (Phase C)
- Pass 166 — CTF sweep increment 6 — synth backends + analysis orchestration (Phase C)
- Pass 167 — CTF sweep increment 7 — CLI / orchestration cluster (Phase C)
- Pass 168 — CTF sweep increment 8 — embedded fade envelope (Phase C)
- Pass 169 — CTF sweep increment 9 — core synth dispatch / internal helpers (Phase C)
- Pass 170 — CTF sweep increment 10 — binary deserialization / converter cluster (Phase C)
- Pass 171 — CTF sweep increment 11 — host GPU-tile preprocess concurrency (Phase C)
- Pass 172 — CTF sweep increment 12 — oscillator asin NaN injection (Phase C)
- Pass 173 — CTF sweep increment 13 — host SIMD quantized domain-guard parity (Phase C)
- Pass 174 — CTF sweep increment 14 — file-I/O + CLI boundary cluster (clean audit) (Phase C)
- Pass 175 — CTF sweep increment 15 — peak frequency-estimation cluster (clean audit) (Phase C)
- Pass 176 — CTF sweep increment 16 — SpectralTracker lifecycle / per-thread storage / OpenMP reduction (clean audit) (Phase C)
- Pass 177 — CTF sweep increment 17 — STFT analysis FFT driver + orchestration cluster (clean audit) (Phase C)
- Pass 178 — CTF sweep increment 18 — CPU additive-synthesis + wavetable + oscillator-math cluster (defect fixed: host SIMD PWM non-finite divergence) (Phase C)
- Pass 179 — CTF sweep increment 19 — GPU synthesis dispatch cluster + cross-backend timbre gate (clean audit) (Phase C)
- Pass 180 — CTF sweep increment 20 — segment-cache persistence cluster (spectral_seg_cache.c, clean audit) (Phase C)
- Pass 181 — CTF sweep increment 21 — Q15 ARM32 fixed-point synth cluster (defect fixed: expired-segment prune ordering drops boundary-aligned partials) (Phase C)
- Pass 182 — CTF sweep increment 22 — host file-I/O layer (spectral_fs.c + spectral_seg_cache_fs.c, clean audit) (Phase C)
- Pass 183 — CTF sweep increment 23 — CUDA tile-parallel synth backend (defect fixed: goto bypasses scalar initializer → ill-formed C++) (Phase C)
- Pass 184 — CTF sweep increment 24 — segment storage + Q15 LUT + CPU fade-envelope + resource bridge (clean audit) (Phase C)
- Pass 185 — CTF sweep increment 25 — support/utility math cluster: hash lifecycle + resource-path canonicalization + alloc/overflow helpers + perf cost-model + Q15 primitives + error/peak-model (clean audit) (Phase C)
- Pass 186 — CTF sweep increment 26 — synthesis backend dispatch + wavetable bank (load/HEX-parse/lookup interpolation) (clean audit) (Phase C)
- Pass 187 — CTF sweep increment 27 — ARM DWT/ITM debug instrumentation: unsigned-EWMA underflow fix (×3) + analysis-proc / processing-chain / console / log / audio-in audit (Phase C)
- Pass 188 — CTF sweep increment 28 — Daisy Seed firmware glue + UART command protocol (clean audit; one deferred SD-load validation observation) (Phase C)
- Pass 189 — CTF sweep increment 29 — tree-wide defect-class cross-cut (unsigned-underflow-shift / integer div-by-zero / computed-size memcpy) + remaining inline-logic headers (clean audit) (Phase C)
- Pass 190 — CTF sweep increment 30 — tree-wide defect-class cross-cut round 2 (float→int out-of-range conversion / signed-left-shift UB / transcendental-domain NaN injection) (clean audit) (Phase C)
- Pass 191 — CTF sweep increment 31 — tree-wide defect-class cross-cut round 3 (strict-aliasing type-pun / signed-integer-overflow in arithmetic) (clean audit) (Phase C)
- Pass 192 — CTF sweep increment 32 — concurrency / floating-point-determinism cross-cut of every OpenMP parallel region (clean audit; one accurately-characterised non-defect note) (Phase C)
- Pass 193 — CTF sweep increment 33 — memory-safety lifecycle cross-cut (use-after-free / double-free / leak-on-error-path / uninitialized read) at the highest-risk cleanup sites (clean audit) (Phase C)
- Pass 194 — Optimisation track F1+F3 — MQ track linkage + cubic-phase interpolation (double-gated, default bit-identical)
- Pass 195 — Optimisation track O4-B — restrict hints on the CPU reduce path (bit-identical)
- Pass 196 — Optimisation track O1-B — CPU output tiling replacing the full private-buffer reduce (bit-identical)
- Pass 197 — Optimisation track O4-A — REJECTED-as-specified (the `df_q15` chirp source quantises to 0 for every realisable input)
- Pass 198 — SIMD oscillator — cubic MQ phase, intuitive comments, and a parity/seam audit
- Pass 199 — Band-limited ("musical") oscillator quality modes
- Pass 200 — SIMD becomes the default CPU oscillator path
- Pass 201 — degree-9 minimax sine becomes the default (scalar + SIMD)
- Pass 202 — U1c — single L1 oscillator kernel (spectral_osc_eval)
- Pass 203 — U1d — codegen the Metal MSL from the C contract
- Pass 204 — U3 — optimize the oversample band-limited renderer
- Pass 205 — U2 — adversarial audit of the optimized band-limited file
- Pass 206 — Q0 — Q-domain type discipline + contract CTest
- Pass 207 — Q2 — width-parameterize the float L1 SIMD oscillator
- Pass 208 — Q3a — Q15-compute precision characterization (measure-first)
- Pass 209 — Q3b slice 1 — opt-in Q15 compute plumbing + production parity lock
- Pass 210 — Q3b slice 2 — measure-first, SIMD Q15 kernel declined on data
- Pass 211 — Q5a — integer-NCO cubic phase primitive + measure-first gate (PASSED)
- Pass 212 — Q5b — integer-NCO phase wired into the production Q15 path
- Pass 213 — Q5c — packed 8×Q15 SIMD oscillator (the double-lane density win)
- Pass 214 — Bv — vectorized uint32 8-wide NCO phase (the deferred Q5c follow-up, scoped on c3==0)
- Pass 215 — expose the desktop Q15 compute domain via `--q15` (make the opt-in kernel reachable)
- Pass 216 — guard the desktop Q15 sine LUT + compute path out of embedded firmware
- Pass 217 — route sine into the packed 8×Q15 SIMD kernel (refactor Thread B1)
- Pass 218 — B2 full-Q15 amp-scale experiment — DECLINED on data (refactor Thread B2)
- Pass 219 — Q-island audit (refactor Thread B3)
- Pass 220 — C2 `spectral_vector_ops` width audit — DECLINED on evidence (refactor Thread C2)
- Pass 221 — full/fused analysis parity harness (CAMPAIGN_2_MASTER_PLAN Phase D — D1)
- Pass 222 — oscillator backend matrix + Q15 contract versioning (Oscillator-Backend-Contract Phase 0 + 1a/1d)
- Pass 223 — CMSIS-Q15 oscillator kernel on the canonical contract (Oscillator-Backend-Contract Phase 2)
- Pass 224 — oscillator backend-contract pin-completeness gate (Oscillator-Backend-Contract Phase 3)
- Pass 225 — vDSP / Accelerate math-acceleration audit (Oscillator-Backend-Contract Phase 4)
- Pass 226 — GPU Q15 / fp16 double-pack audit (Oscillator-Backend-Contract Phase 5)

## ARM/embedded performance campaign (CAMPAIGN_2_MASTER_PLAN A2/A3 + memory-efficiency)
- Pass 227 — honest ARM synth header + assumption-free accumulator-traffic counter (measure-first)
- Pass 228 — exact q63 synthesis accumulator (saturate-once mix; M7 dual-MAC foundation)
- Pass 229 — M7 dual-MAC (SMLALD) voice-pairing for full-sustain pairs (codegen-confirmed)
- Pass 230 — bind memory placement to the real Daisy BSP linker sections (.dtcmram_bss/.sdram_bss)
- Pass 231 — SPECTRAL_HAS_DUAL_MAC capability over CPU macros + libc-free spectral_mem_zero
- Pass 232 — document freq_inc uint32 wrap as intended modular phase aliasing (audit false-positive)
- Pass 233 — DMA-buffer cache-invalidate derived from placement (latent coherency bug)
- Pass 234 — two intensive multi-perspective audits (perf; cache/bandwidth/OpenMP) — design confirmed sound at instruction level
- Pass 235 — coupled-form Q31 recursive oscillator + SNR contract (C1 foundation; magic-circle form rejected on measurement)
- Pass 236 — replace the per-sample LUT gather with the coupled oscillator on the M7 path (C1; deterministic-latency synthesis)

## Campaign 3 — reviewer mandate (S1–S5, REVIEWER_HANDOFF.md)

- Pass 237 — fix on-target DSP build break: __smulbb is not an ACLE intrinsic; widened multiply, SMULBB codegen-verified
- Pass 238 — M7 perf-model measurement stack decided + planned (M7_PERF_MODEL_PLAN.md); codegen rig proven on the real M7 TU
- Pass 239 — perf-model P1: committed codegen census + innermost-loop llvm-mca rig (tools/perf_model); first per-loop M7 numbers
- Pass 240 — perf-model P2: QEMU mps2-an500 counts rig (freestanding runner + TCG plugin); real-kernel dynamic insn/byte counts, bit-reproducible
- Pass 241 — tools audit: bench runner was broken on macOS (BSD time treated as GNU) — probe flavor, BSD -l fallback; fictitious perf positional names → real CLI contract
- Pass 242 — embedded perf stack consolidated into spectral_tools.performance.embedded: one CLI (benchmark_workflow m7-census/m7-counts + CMake m7_census/m7_counts), fixture SSOT generates the runner header, harness under pytest tests/tools; shell rigs deleted; migration verified count/cycle-identical
- Pass 243 — measurement matrix (performance/matrix.py + measure verb): SSOT for instrument-per-build with runtime probes; debugger-as-timing declined on measurement (~3.4 ms/lldb stop vs ~600 ns/marker); ADR-0002 records pipeline, C-truth rule, provenance tagging
- Pass 244 — real newlib toolchain (sha-pinned xPack via m7-bootstrap; brew bare gcc rejected by libc probe); fs_include stubs deleted (census measured identical); runner links -lc_nano + pins FPSCR=0; target flags parsed from options.cmake at runtime
- Pass 245 — QEMU fidelity contract documented + measured (exec cb counts architecturally-executed insns incl. predicate-false IT members; mem cbs architecturally-performed only; FPv5/FPSCR parity to TRM); plugin gains 32B-line working-set metrics + address-trace mode; finished third_party submodule migration (simde/xxHash per libs.txt; 1 GB accidental toolchain commit amended out of history)
- Pass 246 — vendored-dependency rework (ADR-0003): libs.txt → strict YAML manifest with explicit kinds; new spectral_tools.vendor (submodule status/verify/sync/add/remove derived from git state; one CLI front door incl. subtree forwarding); subtree engine rewired onto the manifest via replace_entries(kind) so kinds cannot clobber each other; bypass = sync:false (submodule) / track:none (subtree), enforced one-per-kind; pytest tests/tools/test_vendor.py incl. live manifest⇄git verification
- Pass 247 — adversarial security+correctness pass over tools/ (52-agent fleet, 3-skeptic majority-refute, 8 confirmed / 8 refuted): FIX git option-injection RCE — manifest url could be `--upload-pack=…@x` (passed the old `://`/`@` substring check) and reached `git ls-remote`/`fetch` as a positional, executing arbitrary commands on `vendor subtree status`; now url is transport-allowlisted (https/ssh/git/file/scp form, ext:: rejected), url/ref/path/sparse reject leading `-`, and `--` separators added to ls-remote/fetch/submodule-add as defense in depth. FIX manifest.save() silently dropped an entry on duplicate name (cross-kind collision could erase a submodule); now raises, and subtree names derive from full path. FIX bootstrap trusted a server-fetched .sha on non-darwin platforms (self-certifying); now all four xPack platforms are sha-pinned, runtime .sha fetch removed, fail-closed + partial-download cleanup. FIX codegen loop extraction truncated a loop body on continue-style double back-edges (dormant; measurement-correctness). tools/core_audit rehomed to tests/core_math/harnesses. Regression tests for each.
- Pass 248 — adversarial KERNEL bug sweep (7-lens fleet, 3-skeptic majority-refute, 6 confirmed / 8 refuted), each confirmed defect made a durable test. FIX signed-overflow UB in spectral_osc_quantized ((float)INT_MAX rounds to 2^31; `>` → `>=`) + UBSan ctest osc_formulas_domain. FIX dma_seg_buf not cache-line aligned (DMA invalidate-by-addr could discard a dirty neighbor) + compile-time _Static_assert guard. FIX embedded resource file_id double-canonicalized (FNV(canon(canon)) vs runtime FNV(canon)); canon is not idempotent so find_by_id silently NULLed for repeated-byte-run paths — generator now hashes the raw path, table regenerated, pytest roundtrip test. FIX vDSP window divergence — desktop shipped Apple's PERIODIC Hann while docs/embedded use symmetric N-1; removed the vDSP window path (unify on the documented SSOT) + window_backend_parity ctest. TEST replace a tautological Python two-log/three-log test with compiled peak_interp_parabolic exercising both real branches; harden full/fused harness (dead print-cap → counter, silence asserts count==0). Also found + fixed pre-existing build breaks: 4 parity test targets missing spectral_log.c link (clean-build undefined symbol, masked by stale binaries); tests_all metal-codegen parallel race (gate oscillator-compiling test targets on generate_metal_osc).
- Pass 249 — second (deep) adversarial kernel sweep (7-lens fleet over the largest/least-covered subsystems, 3-skeptic majority-refute, 2 confirmed / 5 refuted), each fixed with a durable test. FIX SIMD quantized oscillator INT_MAX bound used OSC_VCMPLE (<=) vs the scalar's strict >= (the pass-248 twin) — at scaled==2^31 the SIMD lane emits -rads (out of [-1,1]) while scalar returns 0; changed to OSC_VCMPLT, new osc_parity boundary case. FIX Segment endian swap skipped the _pad_w cubic-phase annotation words (latent: needs SPECTRAL_PRECISE_PHASE=1 + big-endian host + cross-endian exchange) — factored an unconditional spectral_segment_swap_words covering all 16 words, new ctest segment_endian_roundtrip. DOCS reconciled window unification in CORE_CONTRACTS, test count in REVIEWER_HANDOFF; authored REVIEWER_HANDOFF_2.md. ctest now 18.

- Pass 250 — REVIEWER_HANDOFF_2 §4 hardening + S1 P3 closed. HARDEN |Segment.width| ≤ 2^24 (SPECTRAL_SEGMENT_WIDTH_MAX) at payload validation — defense-in-depth for the rads*width INT_MAX envelope (pass-248/249 class); new ctest segment_payload_bounds, fail-on-bug verified (ctest 19). DOCS SPECTRAL_PRECISE_PHASE marked EXPERIMENTAL (maintainer decision; old comment claimed a golden-tested path that does not exist; fate decided at F/F3). DATA window convention (§4.4, gather-then-decide): harness window_convention_sweep.c + pytest — periodic = exact COLA (~1e-7) vs symmetric O(1/N); log-parabolic estimation convention-INSENSITIVE (<0.6%, periodic marginally worse); coherent-gain shift ~+1/N; Blackman fails COLA at hop N/2 in BOTH forms; decision stays with maintainer (data favors keep-symmetric, revisit iff F adopts IFFT-OLA). S1 P3 CLOSED: mca_validation.py — 9 hand-analyzable microbenches with expectations derived only from sourced facts (TRM DDI0489F skeptic-verified; quinapalus/jnk0le DWT measurements re-verified first-party; ARM publishes no M7 cycle table — sourced); CortexM7Model body-throughput delta ≤1% on 9/9 (llvm-mca 21.1.8); known systematic: no branch folding, back-edge +1–2 cyc/iter upper bias → per-kernel bounds 0.6% (sustain 340/16-sample) to ~11% (tail 18); CLI m7-mca-validate; mca_region extracted as shared invocation path. Research: 164-agent verified-facts workflow (4 lenses, 2-skeptic refute); session limits killed most skeptics mid-run — load-bearing facts re-verified directly, recorded in mca_validation.FACTS with per-fact verification status. ENV PyYAML loss (Python 3.14 upgrade) had silently module-skipped test_vendor.py — restored; pytest tests/tools 34, tests/core_math 16 green.
- Pass 251 — S1 P4 CLOSED (M7_PERF_MODEL_PLAN): analytical memory-stall layer (memory_model.py, CLI m7-stalls, matrix instruments mca-validation/memory-stalls). Rig: segbuf → .bulk_bss SDRAM mirror + per-block .bulk_marker trace epochs; model: 16KB 4-way WBWA D-cache sim (incl. dynamic-read-allocate approximation) + per-bank open-row tracker over the measured address trace; stalls reported as [bandwidth, serial] BOUNDS (non-blocking M7: 2 linefill buffers + merging store buffer). Every constant carries provenance (libDaisy-as-shipped FMC + clock tree CPU400/AXI200/SDCLK100, TRM DDI0489F, AN4891 Rev1 anchors incl. DTCM zero-latency + D-cache 16KB + cached-SDRAM FFT ×1.19 cross-check); one labeled ASSUMPTION (AXIM→FMC bridge 12 cyc, uncalibrated without a board). Derived: row-hit linefill 56 cyc, row-miss 160, writeback 44. FINDING (upstreamable): libDaisy sdram.cpp ships RPDelay=16/RCDDelay=10 ("started at 2") — ~4× datasheet-conservative; row-miss 160→~70 cyc at datasheet minima. Fixture: all fills in init/load epoch, steady-state per-block SDRAM stalls = 0 (5-line segment store cache-resident), DTCM zero-wait by placement ⇒ mca L2 numbers ARE the block cost for DTCM-resident work. Scaling input for P6/F2: streamed (non-resident) segment scan ≈ 14–25 cyc per segment per block. pytest tests/tools 39 green (5 new model tests + qemu end-to-end epoch contract). AN4891 obtained via eefocus mirror (st.com gated); rev-1 H7x3 edition.
- Pass 252 — constants centralized + best-performance SDRAM config (maintainer directive). NEW api/daisy_seed/daisy_seed_sdram.h = the ONE SSOT for Daisy memory-system numbers (clock tree, FMC SDRAM timing, SDRAM/cache geometry; per-value provenance in-header): memory_model.py parses it at runtime (C-truth, fail-loud on contract move), the counts plugin takes lineshift= from it (hardcoded LINE_SHIFT deleted; plugin errors without it), marker address parsed from the rig ldscript. CHOSEN FMC timing = datasheet-derived minima (worst-of Alliance family "Common Parameters" table + libDaisy-quoted -6 minima): TRP 16→2, TRCD 10→2, TWR 3→2, TRAS 4→5 (shipped 40ns was BELOW the 48ns min — out-of-spec), TRC 8→7; CAS stays 3 (chip mode-register reload not worth untestable 4-cyc gain). daisy_spectral_init() now programs FMC_SDTR1 (apply fn no-op off-target; SPECTRAL_SDRAM_STOCK_TIMINGS opt-out; flag for on-target verification when a board exists). Model effect: row-miss linefill 160→72 CPU cyc (row-hit 56 unchanged); report carries stock-vs-chosen comparison. Header _Static_assert ties SPECTRAL_DAISY_CACHE_LINE to the engine SPECTRAL_CACHE_LINE (M7-gated), syntax-verified under the xPack cross-gcc. ctest 19, pytest tools 40 + core_math 16 green.
- Pass 253 — S1 P5 CLOSED (M7_PERF_MODEL_PLAN): old perf model RETIRED on evidence + WCET delivered. DELETE runtime/spectral_perf_model.c/h — every constant uncalibrated (confidence/pessimism numerology) and the op taxonomy priced a pre-pass-236 kernel (per-sample LUT gather gone; 4×-unroll loop term vs the real 16×): re-deriving constants cannot fix a wrong structure. The sim keeps its real value (runs the REAL spectral_arm32_process); counters reduced to MEASURED quantities only: voice_samples (renamed from the stale lut/mac/phase trio), scan checks, activations, accum_rw_words (the A2 evidence), on-device DWT peak-block; host never fabricates cycles; EmbeddedPerfEstimate = measured workload + budget arithmetic, report points at the stack; env knobs SIM_PERF_PROFILE/SIM_PESSIMISM/SIM_PERF_COLD + setters removed. NEW wcet.py (CLI m7-wcet, P5 done-when): parametric per-block WCET upper bound composing L2 (validated mca worst kernel + back-edge bias) + L1 (measured insn profile, conservatively scaled) + L3 (cold row-miss serial fills; SDRAM price upper-bounds flash), explicit [bound:] labels + unroll-drift guard. Reference: 64 active + 1024-segment scan + 256-block ≈ 1.02M cyc = 48% of the 48kHz budget @400MHz. desktop/simulate/embedded_arm[_float] clean; ctest 19, pytest tools 44 + core_math 16.
- Pass 254 — S1 P6 DECIDED (M7_PERF_MODEL_PLAN): the model applied to the open optimization candidates, three decisions on data. D1 loop-nest/data-layout inversion DECLINED — the halved-accumulator-traffic win prices to ZERO stalls (q63 accumulator is DTCM-resident, zero-wait [TRM/AN4891]; issue slots already in the validated 21.3 cyc/vs; inversion would add per-sample state reload). D2 SMLALD pairing extension DECLINED — pair vs sustain = 20.0 vs 21.3 cyc/vs (6.1% on the paired fraction only) + zero traffic win; existing full-sustain pairing stays. D3 ITCM placement DEFERRED-TO-HARDWARE, quantified — steady-state ≈ 0 (9.9KB working set I-cache-resident, AN4891 ×1.01), real win = determinism (deletes ~22K-cycle I-side cold/jitter term ≈ 5.6% of the 1ms Daisy callback); needs libDaisy linker section + board. D4 F2 embedded pricing — IFFT JUSTIFIED for dense frames: osc-bank 5.45K cyc per dense partial per 256-block [validated] vs structural Q31 iRFFT-512 bound 15-60K cyc → crossover ~3-12 partials; F stream proceeds, first step = vendor CMSIS-DSP + measure arm_rfft_q31 under the counts rig. CAPACITY (Daisy H750 @400MHz/48kHz, m7-wcet sweeps): WCET-guaranteed 32 voices @88% on the 48-sample callback (24@71%), 128 voices @89% on 256-blocks (= DAISY_MAX_ACTIVE cap); expected steady-state ~28 cyc/vs → 128 voices ≈ 43% @48-callback, ~290-voice compute ceiling @256; segments storage-bound at 2M (48MB pool); 96kHz halves budgets, 480MHz boost +20%. Full table + provenance in M7_PERF_MODEL_PLAN.
- Pass 255 — perf contract + F-stream step 1 (maintainer notes 1-4). GATE (notes 2+3): expectations.py = ONE generated baseline (CLI m7-baseline --generate/verify, the resource-hash pattern) freezing per-kernel cycles+insns, measured counts, derived memory costs and the WCET capacity scenarios into tests/fixtures/m7_baseline.json; tolerance bands named+justified in one table (modeled ±10%, measured insns ±5% [0.03% observed cross-toolchain], WCET ±15%, working sets ±64 lines); STONE ceilings asserted independently of the baseline = the published capacity promises (32 voices @48-block ≤100%, 128 @256-block ≤100%, ≤25 cyc/voice-sample); pytest gate (test_perf_gate.py) names the exact quantity that moved; regenerating = deliberate contract re-signing. wcet.py now parses DAISY_SAMPLE_RATE/AUDIO_BLOCK_SIZE/MAX_ACTIVE from daisy_seed_config.h (C-truth; hardcoded 48000 gone). F STEP 1 (note 1): CMSIS-DSP v1.16.2 + CMSIS_6 v6.1.0 vendored (sparse submodules, vendor front door, gitlinks tag-pinned); fft_probe.py measures arm_rfft_q31 (inverse 512, Daisy flags) on the counts rig: 43,910 insns/call [measured] → MEASURED osc-vs-IFFT crossover ≈ 7 dense partials (4-16 cycles band) — structural bound confirmed+tightened; band test guards the input. pytest tools 49 green. NEXT: adversarial audit over passes 250-255 code (note 4), then the IFFT synthesis implementation (desktop reference first, mirroring doctrine).
- Pass 256 — CMSIS-vs-custom-Q evaluation (observables) + inline audit. EVAL: typedefs identical (q15/q31/q63 — coexistence verified both orders, host+M7); Q range macros collided (same values, unguarded in CMSIS) → ours now #ifndef-guarded, -Werror coexistence verified, boundary rule documented at the definition site (CMSIS-DSP = leaf algorithm blocks; engine Q semantics = spectral_q15.h). DECISIVE CODEGEN: arm_sin_q31 = 24 insns + 2 table loads + call overhead PER SINE vs 26.9 insns for our ENTIRE per-voice-sample synthesis (coupled osc + amp + MAC + q63 accumulate, zero memory) [baseline fixture + objdump]; CMSIS has no x86 SIMD backend (our 8×Q15 SIMDe path measured 1.26-1.57× over float-SIMD on host); CMSIS is ARM-only vs the mandate's RISC-V-next portability. VERDICT: keep the Q-domain core (rubric: desktop-path use + measured edge + portability + in-source directive control); adopt CMSIS for leaf algorithm blocks only (FFT measured 43,910 insns; FastMathFunctions cone added for comparison). AUDIT: fleet lost to session limits (re-queued); inline audit found+fixed 1 real defect — expectations.verify() zip truncation silently dropped WCET-scenario checks on STONE_SCENARIOS drift; compare() factored pure + 3 gate-logic tests (fail-on-bug verified); cleared: cache-sim streak granularity (matches TRM per-linefill trigger), fft buffer sizing, SDTR1 bank-1 layout, nm range staleness. pytest tools 52, core_math 16, ctest 19.
- Pass 257 — F-stream F0+F1 (IFFT synthesis). NEW docs/core_audit/IFFT_SYNTHESIS_PLAN.md: phases F1 math-harness → F2 desktop reference (contract header spectral_synth_ifft.h + port-dir TUs per the pass-256 file-org ruling, hybrid density router, opt-in) → F3 golden sign-off → F4 ARM Q31 port (CMSIS rfft, port/cmsis/) → F5 capacity republish; the IFFT path's INTERNAL periodic-Hann synthesis window satisfies COLA exactly without touching the deferred analysis-window decision (§4.4). F1 CLOSED: harness ifft_synth_sweep.c validates the frame-builder construction (circularly-centered real motif, (−1)^k twiddle, Hermitian placement) — COLA machine-exact; MEASURED error floors: frame truncation K=4/8/12 → −43/−55/−63 dBFS (Hann ~1/d³; the plan's −80dB@K=8 guess corrected by measurement), stream OLA RMS −83…−92 dBFS, interpolation negligible at O≥16 (small motif tables fine); per-partial placement cost ~2K complex MACs so K barely moves the measured ~7-partial crossover. pytest gate test_ifft_synth_sweep.py pins floors +3dB headroom. core_math 17, tools 52, ctest 19 green.
- Pass 258 — F2 core LANDED (IFFT_SYNTHESIS_PLAN): contract header synth/spectral_synth_ifft.h (no third-party leakage), backend-independent frame renderer (float F1 formulation, K=8/O=16 measured operating point, motif built at create from the same periodic-Hann the OLA uses), and two host iFFT port TUs per the pass-256 file-org ruling — port/host/spectral_ifft_vdsp.c (zrip inverse, packed half-spectrum, 1/N scale) and spectral_ifft_ref.c (portable radix-2, full Hermitian expansion) — exactly one live per build via whole-file SPECTRAL_USE_VDSP guards. SPECTRAL_PI_D added to the consts SSOT (double pi for init-time tables). NEW ctest ifft_synth_parity (#20, in tests_all): port inverse vs reference iDFT 5.6e-9; stream parity vs exact double oscillator sum −72.8 dBFS max / −87.5 dBFS RMS at 64 dense partials (beats the F1 double-precision floors through the REAL float+vDSP path); byte-deterministic; MEASURED info line: 7.5× over the naive float oscillator loop at 64 partials (consistent with the embedded ~8× pricing). ctest 20, pytest tools 52 + core_math 17 green. Next: F2b hybrid router into engine dispatch (opt-in), F3 golden.
- Pass 259 — S3 kernel-layout refactor: survey + design (KERNEL_LAYOUT_PLAN.md). 6-lens fleet survey (synth tree, backends, core/port+Q files, build wiring, sim usage, GPU coupling) mapped the real include graph. Target tree: core/ (kernel proper, gains q15 + synth API + cpu driver + IFFT renderer) / arch/{ref,arm,simd} (ISA-contingent, capability-selected) / drivers/{metal,cuda,vdsp,cmsis} (device+library backends behind core contracts); synth/ and core/port/ dissolve; boards stay in api/. Survey findings: spectral_q15.h upward-included by FIVE core headers (the central inversion — q15 is de-facto core); GPU anchored to core by 4 couplings (MSL payload #included by core/oscillator.c → dead rodata in all targets + verify gate on all 6; extern decls behind __APPLE__ sniffing in the public API header; unchecked MSL struct mirrors; vendor-neutral plan/caches correctly in core); sim KEEPS narrow value (only E2E host pipeline + only exerciser of the float→Q15 conversion policy) → becomes arch/arm host adapter after surgery (extract conversion policy, move report printing out, delete dead API+define). BUGS found: measure --target simulate resolves the DESKTOP binary (glob mismatch — matrix row silently wrong); cmd/main.c bypasses the vtable for GPU cleanup; GPU timbre cap tripled. Framework: contract headers in core, build-system TU selection + single capability map, include-direction enforced by a structural DAG test, parity test per port pair (GPU currently has ZERO coverage). Phases L0-L5, each gated by ctest+pytest+the m7-baseline perf gate (moves must be codegen-identical). Execution on maintainer ratification.
- Pass 260 — Major-Patchset Review instituted. AI.md gains the standing 14-point review checklist (duplication, wiring, constants/macros, architecture+honesty-of-foundations, bug escape analysis, comment doctrine, file structure, library use, seven-lens panel, honesty ledger, test quality, truthful naming, lifecycle/boundaries, docs-in-patchset) — applied at every PR-sized unit / ~10 commits / before structural plans; K&R as the style north star. NEW docs/core_audit/MASTER_REVIEW_PLAN.md: the first-instance execution plan, seeded with the observed-evidence ledger (R1-R10): conversion-policy dup, GPU timbre cap ×3, unchecked MSL mirrors, rig-helper/PRNG dup, the simulate-binary glob bug + per-target resolution class test, dead sim API/define, zero GPU test coverage, multi-eval saturating macros + powf-in-macro, synth_cpu rename macro, empirical constants needing provenance, baseline fixture absolute-path, method-token silent no-ops, and the conversational-reference comment scrub (the AI.md:78 rule, violated by recent code — full grep list). Waves W1-W7, each commit-sized and gated by ctest+pytest+m7-baseline; runs BEFORE KERNEL_LAYOUT_PLAN execution so the layout move relocates no debt.
- Pass 260 addendum — K&R instituted as the golden C guide: the in-repo "C Programming Language" 2nd ed. (docs/core_audit/, deliberately untracked binary) is now review item 11 in the AI.md checklist ("adherence without mercy"; items renumbered to 15) and R11 in MASTER_REVIEW_PLAN (eighth lens with veto in W6 + dedicated hot-kernel idiom sweep). AI.md specificity audit: the perf-CLI verb enumeration (already stale — the over-specific failure mode in action) replaced with --help/measure-list discoverability; the lldb per-stop microsecond detail reduced to the principle with the ADR-0002 pointer; a stale line-number doc reference in the review plan replaced with the rule's name. Balance otherwise judged sound: stable SSOT lists and hard rules stay specific (useful specificity), principles stay general.
- Pass 261 — Master Review W1 (comment scrub) LANDED. All conversational/change-history references purged from code comments across 33 files (engine, api, tools, tests, cmake): pass-number citations rewritten to the harness or doc that owns the measurement (PASS208 floors → q15_compute_precision; PASS210/213 economics → bench_q15_pack8/QTYPE_DOMAIN_PLAN; pass-248/249 → the INT_MAX defect class + its reproducer test; pass-253 retirement → described, not dated), "maintainer decision <date>" → the policy + governing doc, "signed off" → what the test actually pins, mca FACTS verification vocabulary de-jargonized ("skeptics" → "corroborated", test updated in lockstep), version-log datestamp dropped from the osc_q15 contract header, one redundant change-narrative paragraph deleted (synth_cpu.c). Fixture id campaign3-p2-9voice deferred to W3 (compiled into the generated workload header — not provably codegen-neutral in W1). Verified: ctest 20/20, pytest 69, m7-baseline gate PASS (live stack identical — scrub proven codegen-neutral).
- Pass 261 — Master Review W2 (wiring + dead code) LANDED. BUG FIXED: `measure --target simulate` resolved the DESKTOP binary (shared `spectral_*_desktop` glob) — binary resolution is now per-target, with the glob DERIVED from the CMakeLists `set(TARGET_* ...)` naming SSOT (C-truth; fail-loud parse), `TargetProfile.binary_cmake_var` binds profile→name. CLASS TEST tests/tools/test_measure_matrix.py (5 tests): profile→OUTPUT_NAME binding parsed from the target cmakes, glob matches own name and NO other (ambiguity = fail), workflow-level per-target resolution with both binaries present, loud KeyError for non-host profiles; binding test fail-on-bug verified. Dead code deleted: embedded_sim_set_config/verbose (zero callers), SPECTRAL_SIMULATION_TARGET_DAISY (zero consumers). bench_metal_q15pack.m wired as Apple-gated manual target (was "not in CMake" limbo; runs, reproduces the archived double-pack audit numbers). NEW ctest simulate_smoke (#21): the simulate binary now RUNS end-to-end (WAV→analysis→Q15 conversion→real arm32 kernel→WAV) in an isolated build-tree workdir, asserting real audio output — CI built it but never executed it. NEW ctest gpu_backend_parity (#22): first automated GPU coverage — Metal-vs-CPU through the real CLI on sine+saw, bounds max<1e-5/RMS<1e-6 vs measured floors −132/−144 dBFS (~30 dB headroom), exit-77 visible skip when no Metal device (CPU fallback detected, never a silent pass); fail-on-bug verified. CI: pytest job now runs tests/tools (was never in CI) with pyyaml (kills the silent test_vendor module-skip). host-config Metal/Foundation REQUIRED justified in place (desktop unconditionally defined on Apple). ctest 22, pytest 74, m7-baseline gate PASS.
- Pass 261 — Master Review W3 (macro hygiene + constants provenance) LANDED. Multi-eval/function-hiding macros → static inline (codegen-neutral, gate-proven): SPECTRAL_SAMPLE_{TO_FLOAT,ADD,MUL} + FLOAT_TO_SPECTRAL_SAMPLE (both embedded-Q15 and desktop-float branches of spectral_config.h) → spectral_sample_{to_float,add,mul} / float_to_spectral_sample; SPECTRAL_PITCH_FACTOR (powf hidden in a macro) → spectral_pitch_factor; all call sites renamed (wavetable, synth_cpu, synth_internal, q15 boundary-rule doc). MIN/MAX/CLAMP stay type-generic macros with the multi-eval hazard + #ifndef rationale now written at the definition. Provenance comments added ([chosen: rationale] form): SPECTRAL_GPU_TILE_SIZE 512, SPECTRAL_TRACK_CANDIDATE_BATCH 128, SPECTRAL_NORMALIZE_HEADROOM 0.95, SPECTRAL_SIMULATION_HEADROOM 0.99, SPECTRAL_ARM32_MAX_ACTIVE 512 (storage vs WCET-cap distinction), DAISY_MAX_SEGMENTS_SAFE 2M, DAISY_MAX_ACTIVE 128 (WCET-gated, raise only with new proof). Daisy SDRAM pools (48+8+8 MB) now ENFORCED to tile the 64 MB exactly via _Static_asserts + NEW ctest daisy_config_layout (#23) host-compiles the header (firmware target needs absent BSP; sabotage-verified the assert fires). Harness-local PI annotated with its SSOT mirror. Committed-fixture hygiene: m7_baseline.json meta now stores "arm-none-eabi-gcc 15.2.1" / "llvm-mca 21.1.8" (basename+version) instead of absolute machine paths; fixture id campaign3-p2-9voice → stagger9-8k (workload-descriptive); baseline deliberately regenerated — diff is EXACTLY {fixture_digest, two meta ids}, all measured numbers identical. ctest 23, pytest 74, gate PASS.
- Pass 261 — Master Review W4 (duplication hoists) LANDED. tests/support/check.h: the byte-identical CHECK macro + g_fail flag hoisted out of 14 contract tests (header owns both; record-and-continue semantics documented). tests/support/xorshift_rng.h + native/qemu/rig_support.h: the xorshift32 step (explicit state — every seed/reseed and thus every LOAD-BEARING pinned sequence byte-identical) and the rig's semihosting helpers (SYS_WRITE0 + fixed-width hex) hoisted out of 4 TUs; rig counts proven unchanged (gate PASS). GPU timbre cap: SPECTRAL_GPU_MAX_TIMBRE defined once in spectral_synth_internal.h with the why (width-parameterized timbres have no MSL/CUDA port); the 2 vtables + predicate + fallback router all consume it. ELF-builder unification DECLINED with rationale written at the definition: the two builders share only the link tail; the runner needs a per-TU compile plan, and reshaping the published FFT-probe build would re-baseline its measurements for no gain. segment_to_q15-vs-convert_segments comparison did NOT yield a hoist — it yielded a BUG (recorded R5, fix leads W5): convert_segments scales da/df by 1/1000 against the per-sample unit contract proven at every other stage (analysis inv_hop, desktop synth, arm32 kernel, sim); typical fades quantize to da_q15==0. ctest 23, pytest 74, gate PASS.
- Pass 261 — Master Review W5 (honesty items + bug-class tests) LANDED. BUG FIXED: convert_segments scaled da/df by 1/1000 against the per-sample unit contract (analysis inv_hop → desktop synth → arm32 kernel amp_q15+da_q15*offset all per-sample); typical fades (|da|~2e-4) quantized to da_q15==0 — offline .spq lost fades/chirp entirely. NEW ctest convert_segments_units (#24): runs the REAL binary on a generated SPEC file and asserts every encoded field against a labeled independent Python mirror of the boundary macros (sanctioned verification dup); fail-on-bug verified (re-introduced /1000 → "da_q15: got 0, want -6"). Stub honesty: serra_smith_1990/johnston_1988 no-op stages DELETED; spectral_process_chain_apply now FAILS LOUDLY (unconditional, names the tokens) on any requested-but-unimplemented stage — the default-off SPECTRAL_PROCESS_STRICT switch is gone (honesty is not a build option); CLI help now says Implemented vs Reserved; verified live (-pm johnston_1988 → "Processing stage(s) not implemented: johnston_1988", pipeline aborts; adaptive_track_density still runs). Keep-alive hack (if ref[0]==42.0f) → volatile sink AFTER the timed region (the volatile-array alternative was rejected: it would deoptimize the baseline loop and inflate the reported speedup). __import__ hack → plain import. IFFT full-half-spectrum memset per frame: honesty comment with the arithmetic (2KB/frame vs ~2K-tap placement; 7.5x measured anyway; dirty-region clear deferred to the ARM port). Baseline generate() now runs the reproducibility double-run (a golden must be proven deterministic at signing; per-commit verify stays fast). zip() sweep: gate element-compare now under else-of-drift + strict=True (the bare strict=True variant was caught by the gate's own unit test turning a NAMED failure into a traceback — fixed properly); other zips already strict. ctest 24, pytest 74, gate PASS.
- Pass 261 — Master Review W6 (lens panel + architecture sweeps) LANDED. Capability-not-CPU sweep: CLEAN — every raw __APPLE__/__ARM_ARCH test outside the capability map is either an L4-queued GPU anchor, the arch-contingent arm TUs themselves, or POSIX-availability in the host fs layer. Include-graph scan: CLEAN beyond the two known queued inversions (q15→L1, backend→synth→L3); analysis/ and runtime/ have no upward edges. Dead-#if sweep: SPECTRAL_HAS_DMA found as the one undocumented dormant configuration — and it had ALREADY ROTTED (__DSB unprovided outside a CMSIS firmware build); fixed (guarded same-encoding fallback + dormancy note naming the BSP port contract and the on-target fate decision) and pinned by NEW pytest test_dormant_dma_branch_still_compiles (cross-compiles the kernel TU with the DMA config forced, DTCM placement; caught the rot on its first run). IFFT renderer panel (math/senior/embedded/K&R lenses): boundary-validation defect FOUND+FIXED — render() floor-cast unvalidated float bins to int (the same int-conversion-UB class as the Segment width defect); partials now validated once at the boundary (bin in (1,n_fft/2-1), finite amp/phase, NaN-rejecting comparisons) with 4 rejection CHECKs added to ifft_synth_parity; motif edge math verified exact at d=±K; ports' destroy(NULL) verified safe. GPU one-shot tile cache: justified-in-place verdict (key omits segment identity by design → one-shot handoff + layout-word validation; persistent design would be speculative machinery). Provenance-labels-travel: verified live (m7-wcet report carries [modeled]/[measured]/[bound] on every term). HONESTY (item 10): not covered this wave — full K&R idiom read of spectral_synth_arm32.c + oscillator_simd kernel bodies (their structure is census/mca-pinned, idiom review deferred), memory_model.py + cli_pipeline R7 cohesion read, MSL mirror codegen (L4 carries it). ctest 24, pytest 75, gate PASS.
- Pass 261 — Master Review W7 (re-verify + review-the-review) CLOSED; the first Major-Patchset Review instance is COMPLETE. Adversarial re-read of all six wave commits (93 files, +2151/−360): sample-op inline conversion re-verified semantics-identical (same saturation values, same NaN rule, no wider-typed call sites that the old type-generic macros would have accepted differently); per-target binary resolution re-read clean; embedded_arm_restricted (the one target no wave rebuilt) built clean. Full ladder green: ctest 24/24, pytest 75, m7-baseline gate PASS, all six build targets compile. Review instance totals — 3 BUGS found+fixed+class-tested (simulate-glob wrong-binary, convert_segments /1000 unit defect, IFFT unvalidated-bin UB), 1 rotted dormant config repaired+pinned (DMA __DSB), 5 new test surfaces (ctest 20→24 + the DMA compile check), 2 justified declines written in place (ELF-builder hoist, Metal REQUIRED), zero conversational references left in code, every flagged constant carrying provenance, frozen fixtures machine-path-free. KERNEL_LAYOUT_PLAN L0+L1 may now begin on a clean foundation.
- Pass 262 — KERNEL_LAYOUT L0+L1 LANDED. L0 (last survey fix; W2/W4 absorbed the rest): main.c's HAS_METAL/HAS_CUDA cleanup sniffing → spectral_backend_cleanup_all() on the vtable (missing backends resolve to the fallback no-op; AUTO/EXPORT stay virtual). L1: spectral_q15.{h,c} MOVED synth/math → core (the include graph already treated it as core — five core headers consumed it upward; the placement was the lie). Rewired: source-manifest (SYNTH_MATH_DIR deleted, sources → CORE_DIR, include-root list shrunk), 8 test/bench cmakes dropped their now-redundant synth/math include lines, daisy_seed_config.h relative include, toolchain.py ENGINE_INCLUDE_SUBDIRS + KERNEL_TU_RELPATHS (the rig re-derived its TU path through the C-truth parse). synth/math/ no longer exists; zero path references remain. Move proven codegen-identical: all 6 targets build, ctest 24/24, pytest 75, m7-baseline gate PASS (identical counts). Next: L2 arch/ stand-up (port/host SIMDe TUs + port/embedded + backends/arm + sim post-surgery).
- Pass 262 — KERNEL_LAYOUT L2 LANDED (arch/ stand-up; core/port and synth/backends/{arm,sim} dissolved). Moves: arch/simd/ ← port/host SIMDe TUs (oscillator_simd.c + kernel.inc + scalar_waves.h, vector_ops, out_kernels, gpu_tile); arch/ref/ ← port/embedded profile bodies (gpu_tile stub, out_kernels scalar+capability-gates) + the portable radix-2 spectral_ifft_ref.c; arch/arm/ ← synth/backends/arm (arm32 kernel + debug) + the CMSIS oscillator body (ex port/embedded) + the sim host adapter (post-L2a surgery); drivers/vdsp/ ← spectral_ifft_vdsp.c (created early so core/port retires NOW, not at L4); core/spectral_mem.h ← port/spectral_mem.h (placement contract stays kernel-side). Rewired: manifest dir vars (ARCH_REF/ARCH_ARM/ARCH_SIMD/DRIVERS_VDSP replace PORT_HOST/PORT_EMBEDDED/BACKEND_ARM/BACKEND_SIM), 9 test/bench cmakes, osc_backend_contract scan doc, daisy api relative includes, codegen.py/counts.py/toolchain.py/test_embedded_perf.py rig paths, all cross-reference comments. L1 lesson applied: swept literal paths AND cmake variable forms. AI.md layout map trued to the kernel/arch/drivers shape. All 6 targets + ctest 24/24 + pytest 75 + m7-baseline gate PASS (kernel TU now compiled from arch/arm with identical counts). Remaining: L3 (synth dissolution: api header + cpu backend + IFFT files → core, synth_cpu redirect fix), L4 (gpu → drivers + 4 anchor cuts + MSL codegen + parity), L5 (include-graph layering test).
- Pass 262 — KERNEL_LAYOUT L3 LANDED (synth/ dissolved to gpu-only). The language-changing `#define synth_cpu synth_arm32_simulation` rename macro is DEAD: the simulation TU now DEFINES synth_cpu directly (build-system TU selection — the float OpenMP body compiles itself out under SPECTRAL_USE_EMBEDDED_SYNTH exactly as before), and the public API header no longer includes the arch/arm header (the api→arch inversion is gone; the sim TU includes the contract it implements, the correct direction). Found+fixed in the process: the pipeline's sim-report block compiled on NO_PERF profiles only via the old macro include chain — its guard now matches the perf include (SPECTRAL_IS_EMBEDDED_SIM && !SPECTRAL_NO_PERF). Moves: core/spectral_synth.h ← synth/api (the public desktop API, future spectral_kernel.h), core/spectral_synth_cpu.c ← synth/backends/cpu (portable kernel code), core/spectral_synth_ifft.{h,c} ← synth/ (the F2 renderer + port contract). synth/ now holds ONLY backends/gpu (L4 lifts it into drivers/). Manifest vars (SYNTH_API_DIR/SYNTH_BACKEND_CPU_DIR deleted), ifft cmake, toolchain.py include roots, AI.md layout + public-API pointers, api/README all rewired. All 6 targets + ctest 24/24 + pytest 75 + gate PASS.
- Pass 262 — KERNEL_LAYOUT L4a-c LANDED (gpu → drivers/{metal,cuda}; anchors a+b cut; synth/ no longer exists). Moves: drivers/metal/ ← spectral_synth_metal.m + the generated MSL header; drivers/cuda/ ← spectral_synth_cuda.cu. Anchor (a): the MSL payload include left core/oscillator.c for a dedicated drivers/metal/spectral_osc_metal_payload.c — VERIFIED with strings(1): shader source now absent from the simulate binary (was dead rodata in every target), present only in desktop; the codegen verify gate shrank from all six targets to desktop alone. Anchor (b): HAS_METAL/HAS_CUDA + all GPU externs left core/spectral_synth.h for per-driver public headers (drivers/metal/spectral_metal.h, drivers/cuda/spectral_cuda.h) — each capability decided next to the code it gates; core/spectral_backend.c (the static registry) is the ONE sanctioned core→driver include edge, documented at the include site; cmd consumes the driver surfaces directly for capability text + the VRAM line. Anchor (d) was already correct (vendor-neutral plan/cache stays in core). gpu_backend_parity passes post-move (the relocated driver JIT-compiles and matches CPU). Manifest: DRIVERS_{METAL,CUDA}_DIR vars, payload TU added, driver headers in the include roots. AI.md layout trued. All 6 targets + ctest 24/24 + pytest 75 + gate PASS. Remaining L4d: MSL struct-mirror codegen.
- Pass 262 — KERNEL_LAYOUT L4d LANDED (anchor c: MSL struct mirrors codegen'd). metal_osc.py now parses SegmentGpu (spectral_common.h) + GpuSynthParams/TileRange (spectral_synth_internal.h) and emits gpu_structs_metal_source into the generated header — the hand-written "must match, no compile-time check possible" mirror in spectral_synth_metal.m is DELETED (the generated block came out byte-identical to it, confirming no drift had occurred). Parser is fail-loud by construction: only float/uint32_t have MSL mappings (a new field type forces a deliberate decision), struct bodies are restricted to flat scalar lists (a nested struct/union stops the build rather than generating something plausible — the first regex draft over-matched across the 64-byte Segment's union and the loud failure caught it immediately). Layout equivalence holds because both sides are pure sequences of 4-byte scalars: matching order IS matching layout. Codegen inputs + runner template extended; verify_metal_osc sabotage-checked (corrupted field name in the committed header → verify FAILS; noted: the generate custom command treats a hand-edited NEWER output as fresh — direct module run regenerates). gpu_backend_parity passes with the generated structs (JIT shader matches CPU). ctest 24/24, pytest 75.
- Pass 262 — KERNEL_LAYOUT L5 LANDED; CAMPAIGN COMPLETE. NEW tests/tools/test_layering.py: the include-direction law as an executable dependency-DAG contract — parses every engine/api include edge (365 resolved at adoption, 160 cross-layer, all legal), resolves in-repo headers by basename (duplicate basenames fail: resolution relies on uniqueness and ambiguity is its own smell), and fails on any edge the layer law forbids (kernel=core+runtime → kernel only; arch/X and drivers/X → kernel + own subdir, never each other; analysis → kernel; cmd unconstrained; api → kernel+arch). The two spectral_backend.c registry edges are the documented allowlist. Sabotage-verified (a core→driver include fails by name). Include-dir narrowing deferred with written rationale (the test enforces the contract per commit; narrowing is compile-time ergonomics). AI.md carries the law. Final layout: core/ + arch/{ref,arm,simd} + drivers/{metal,cuda,vdsp} + analysis + runtime + cmd; synth/ and core/port/ no longer exist; every move was gate-proven codegen-identical. ctest 24/24, pytest 76, gate PASS.
- Pass 263 — Embedded resource-separation audit (S1, embedded path only). NEW docs/core_audit/EMBEDDED_RESOURCE_SEPARATION_PLAN.md: a 6-lens adversarial audit (FPU/ALU separation, float/Q mixing, cache coherency, bandwidth, ARM-vs-host arch separation) ledgered with per-finding disposition. Core diagnosis: the per-sample recurrence is pure Q31/Q15 ALU, but the ONLY FP work — the coupled-oscillator seed (spectral_coupled_init, DOUBLE) — runs PER BLOCK PER VOICE (not "activation-time, rare" as the header claimed), an unaccounted FPU burst at every block boundary with zero FPU/ALU overlap; the cheap ALU drift-fix (spectral_coupled_renorm) that would make the seed once-per-activation is test-only. Honesty: on a serial recurrence + in-order core, "saturate FPU and ALU simultaneously" needs overlapping work items (a dual-domain voice split behind the reserved-but-inert SPECTRAL_EMBEDDED_FLOAT), not the current single stream — measure before building. DOC-TRUTH FIXES landed (comment-only, m7 gate untouched): arm32.c file header rewrote the stale "placement is INERT / .dtcm_data" block (the Daisy BSP binds SPECTRAL_MEM_FAST→.dtcmram_bss, so accum+ctx ARE in DTCM) + recorded the FPU/ALU resource model + the dual-MAC-is-realized truth + the q63-width contract at the accum decl; osc_q31.h corrected "rare/float-minimax"→"per-block/double-Taylor" and the renorm-vs-reseed SNR attribution; set_stretch marked intentionally inert (synth-time param, not a runtime knob). AI_CANON gains rules 21-25 (FPU/ALU temporal ownership; barrier≠cache-maintenance; budget caps enforced at the boundary; host-sim compiles the firmware's capabilities + firmware purity proven against the source set; runtime controls fail loud) + a rule-6 clause (placement claims are build-config-specific objdump-checkable facts). Confirmed-but-gated items (PERF-GATED/API/ON-TARGET, maintainer-sequenced): seed-once-at-activation+carry-state (fpu-01/03), DAISY_MAX_ACTIVE=128 enforced nowhere vs runtime 512 (kr-02), dead 8KB LUT required-non-null on M7 (bandwidth-01), 48-sample WCET scenario (fpu-02), production-regime SNR test (arch-03), dormant SCB-invalidate untested (cache-02), firmware-purity test (arch-05), SD-load f_read-into-cacheable-SDRAM dsb-not-invalidate (on-target). Refuted: M7 prefetch-is-no-op (PLD emits), q63→q31 narrowing, output-buffer/clean-before-DMA/false-sharing hazards. Doc-only pass: no code/codegen change, m7 baseline untouched.
- Pass 264 — Embedded audit follow-on: production-regime SINAD gate (arch-03). The coupled oscillator's 72 dB SNR contract (test_osc_recursive) was validated only under the per-block RENORM regime; the SHIPPED arm32 path uses per-block exact re-seed (no renorm) and had no audio-quality gate — only freq/peak tracking, which pass through harmonic distortion. arm_core_test::test_single_tone now measures coherent SINAD over the sustain region at the kernel's EXACT rendered frequency (freq_q88 * phase_inc_scale_q24 / 2^32 * 2pi — phase-agnostic cos/sin least-squares fit, so leakage can't optimistically inflate it): MEASURED 80.7 dB, floor 70.0 (fail-on-bug verified — 2% second-harmonic injection drops it to 28 dB and the gate trips). This is the safety net required before the plan's Phase-A drift-regime change (seed-once-at-activation + ALU renorm). Test-only; ctest 32/32, no engine/codegen change, m7 baseline untouched.
- Pass 265 — Embedded audit follow-on: DMA-invalidate range arithmetic made host-testable (cache-02). The cacheable SCB_InvalidateDCache_by_Addr DMA-RX branch (the one cache-coherency-critical block) was compiled/tested by NO build — the dormancy test forces the DTCM sibling config, which preprocesses the SCB body out. Extracted the line-round + overflow guards into a pure host-testable inline spectral_cache_invalidate_range (spectral_mem.h: begin rounded down, end rounded up, address-space-overflow + non-power-of-two-line + >INT32_MAX guards); the dormant DMA path now calls it (behaviorally identical) and only the SCB call stays firmware-only. NEW arm_core_test::test_cache_invalidate_range pins it (aligned/misaligned/round-up/zero/null/non-pow2-line/INT32_MAX/overflow cases); fail-on-bug verified (regress end-rounding to round-down -> 0x1000,32 not 64 -> trips). No measured-codegen change (DMA path is SPECTRAL_HAS_DMA-dormant, helper unused in measured builds) so the m7 baseline is untouched; DMA dormancy compile + perf-gate fixture checks + ctest 32/32 all green.
- Pass 266 — Phase A step 1 (fpu-03): hoist the coupled-oscillator rotation constants to voice activation. cos_w/sin_w = cos/sin(omega) depend only on the constant phase_inc (non-chirp), but synth_segment_m7/synth_core_pair_m7 recomputed them every block via two f64 degree-15 minimax sines per voice. Split spectral_coupled_init into spectral_coupled_freq_constants (per-activation) + spectral_coupled_seed_state (per-block, the (c,s) drift resync); init now composes both (byte-identical). Active record (AoS SpectralActiveSegQ15 + SoA SpectralActiveSoA) gains cos_w/sin_w (#if SPECTRAL_ARM_M7); the activation scan computes them once and stores; the two M7 render kernels take them as params and only re-seed (c,s); prune carries them; the perf-model wrapper (kernel_wrappers.c) computes them outside the measured region. RESULT: BYTE-IDENTICAL audio — m7 baseline qemu-render digest unchanged (07f868f5ac35d606) and host arm_core_test output identical (peak 0.5000, SINAD 80.7, fades) — with process_insns 3,762,455 -> 2,101,357 (-44%): measured proof the per-block double-precision re-seed dominated the embedded synth instruction count. Steady-state worst cyc/voice-sample 24.01 unchanged (loop bodies untouched). m7 baseline re-signed: diff is ONLY the 4 GCC label keys (.L492/494/574/664 -> .L422/424/504/594) + SAMPLES_PER_ITER (wcet.py) + process_insns; per-loop cycles/iter identical. Live perf gate + ctest 32/32 + embedded host targets green. Next (fpu-01): carry (c,s) across blocks + ALU renorm to drop the remaining per-block seed — AUDIO-CHANGING, rides golden sign-off (SINAD gate arch-03 is its safety net).
- Pass 267 — Phase A step 2 (fpu-01) DECLINED on data. Implemented carry-(c,s)-across-blocks + one ALU spectral_coupled_renorm/block (dropping the per-block f64 state-seed entirely): active record gains osc_c/osc_s, synth_segment_m7/synth_core_pair_m7 carry the oscillator instead of re-seeding, renorm once per block, kernel_wrappers.c + SAMPLES_PER_ITER re-pinned, baseline regenerated. VERIFIED green (digest IDENTICAL 07f868f5ac35d606 for the workload — carry+renorm drift stays sub-q15; host SINAD 80.7 unchanged; live perf gate + ctest 32/32). But the measured gain on the STONE workload was only process_insns 2,101,357 -> 2,075,788 = -1.2%, while it costs +4 KB DTCM (osc_c[512]+osc_s[512]) and replaces the exact-by-construction re-seed with a drift-bounded regime (byte-identical for tested inputs, not by construction). REVERTED per the minimal/decline-on-data discipline: fpu-03 already removed the dominant per-block FP (-44%), and the remaining seed_state was not the bottleneck; the +4 KB DTCM is a poor trade for -1.2% on a 128 KB-DTCM target. Recreatable caveat recorded in EMBEDDED_RESOURCE_SEPARATION_PLAN: the benefit scales with partial DURATION (STONE is transient-heavy), so a sustained-content WCET scenario (fpu-02) could justify revisiting. Tree restored to fpu-03 (26f8314); no code change committed this pass (docs-only: plan + changelog record the decision).
- Pass 268 — Empirical re-measure of fpu-01 at the real Daisy block size (the test the maintainer asked for). The counts fixture stagger9-8k is already SUSTAINED (9 voices x 8192 samples = 32 blocks each), so voice duration was not the variable; BLOCK SIZE is. Measured process_insns on the fpu-03 tree across blocks via qemu counts (custom fixtures): 256→2,101,357 / 128→2,164,294 / 64→2,289,042 / 48→2,373,319. Linear fit process_insns = 27.65·samples + 217.2·renders (renders = 9·⌈8192/block⌉) validates to 0.02% (block=128 predicted 2,163,919 vs 2,164,294). The synth is LOOP-BOUND (27.65 insns/sample); per-render overhead is 217 insns, of which fpu-01 removes ~89 (the f64 state-seed net of renorm). So fpu-01's benefit = 1.2% @256 (the fixture, why it was first declined) but 5.76% @48 (the REAL DAISY_AUDIO_BLOCK_SIZE) — the per-block seed is paid 5.3× more often. TWO findings: (1) fpu-01's −1.2% was a fixture artifact; at the real block it is −5.76% for +4 KB DTCM (compute-bound target) → recommend RE-LAND. (2) fpu-02 fidelity gap CONFIRMED: the canonical fixture renders at block=256 but the device runs 48, under-pricing per-block overhead by +12.9% (2.10M→2.37M); the fixture should measure at the real block. Docs-only this pass (plan + changelog record the evidence + recommendation); the re-land of fpu-01 + the SSOT-fixture reshape (fpu-02) await the go (reverses a committed decline + reshapes the perf SSOT). Tree at fpu-03 (26f8314), gate green.
- Pass 269 — Phase A: fpu-01 (carry-(c,s)+per-block-renorm) LANDED together with fpu-02 (fixture at the real Daisy block), on the 512-voice real-time mandate. fpu-01: the coupled oscillator now CARRIES (c,s) across blocks (active record gains osc_c/osc_s) with one ALU spectral_coupled_renorm/block, dropping the per-block f64 state-seed entirely; render kernels take the oscillator in/out, prune carries it, kernel_wrappers seeds outside the marked region. The shipped drift regime is now carry+renorm (an AUDIO change vs exact re-seed — validated: osc_recursive 86-93 dB SNR/8s, arm_core_test SINAD 80.7 dB, new golden render checksum 0ed6e0ac). fpu-02: the canonical counts fixture (stagger9-8k) renders at block=256 but the device callback is DAISY_AUDIO_BLOCK_SIZE=48 — measured to under-price per-block overhead +12.9% (process_insns 2.10M@256 → 2.37M@48); the fixture is now stagger9-8k-b48 (total 16416 = 48·342). MEASURED at the real block: fpu-01 = −5.96% process_insns (2,373,319→2,231,920); the prior −1.2% @256 was a fixture artifact. Steady-state worst cyc/voice-sample 24.01 unchanged; m7 baseline re-signed (label keys + SAMPLES_PER_ITER .L352/354/434/524 + new fixture digest/checksum). 512-VOICE FINDING: the per-voice oscillator bank cannot hit 512 real-time @48kHz (≈1.0-1.5× over budget; bottleneck is the 24 cyc/voice-sample loop, not the per-block overhead) — 512 voices = dense spectrum = the IFFT (Rodet-Depalle) synthesis regime (F2 fork). So the 512-slot SoA sizing is CORRECT and kr-02's "enforce 128" is reframed (128 is the stale old cap). Verified via arm_core_test + osc_recursive + osc_sin_init_parity + the live perf gate (7/7); full tests_all build is blocked by a concurrent agent's unrelated broken spectral_osc_bandlimited.c (perf_track_realloc undefined) — not this change.
- Pass 270 — CLI output WAV is now uniquely named instead of always clobbering one "out_c.wav". The render path hardcoded SPECTRAL_OUTPUT_WAV_NAME, so every run (any input) overwrote output/out_c.wav. New pipeline_unique_output_wav (spectral_cli_pipeline.c) names the output after the input file's stem (<stem>.wav), and if that exists uses the first free <stem>_N.wav, so different inputs land in different files and re-running the same input never overwrites a prior render; falls back to the resolved default if the stem can't be derived. Two consumers that assumed the fixed name were decoupled to glob the produced WAV: simulate-smoke-run.cmake and tests/core_contracts/gpu_backend_parity.py. (The QEMU perf rig writes NO audio file — it emits CHECKSUM=/RENDERED= over semihosting only.) Verified: desktop + all 4 embedded profiles build clean; simulate_smoke + gpu_backend_parity pass; live run produces <stem>.wav then <stem>_1.wav on repeat.
- Pass 271 — Listen to the QEMU result: a Q15→WAV utility + a render harness that dumps the real M7 audio, with the counts rig kept counts-only. NEW spectral_engine/core/spectral_wav.h: freestanding 16-bit-PCM (Q15) WAV writer — spectral_wav_pcm16_header() builds the 44-byte header with pure byte stores (no libc, usable in the QEMU/embedded freestanding build) and a host spectral_write_wav_q15() (stdio, SPECTRAL_HASH_HAS_HOST_FILE_API-gated). Q15 IS 16-bit LE PCM so the dump is the exact bytes the embedded path produced (no float round-trip); distinct from spectral_io.h's libsndfile float writer. NEW qemu_render_main.c (sibling to the untouched qemu_main.c): renders the SAME fixture through the SAME spectral_arm32_process and writes the rendered Q15 to a host .wav over semihosting; rig_support.h gains semihosting host-file I/O (SYS_OPEN/WRITE/CLOSE — open_wb/write/close). counts.py _build_runner_elf parameterized with main_src (counts elf name preserved). NEW audio.py: render_wav() + CLI `python -m spectral_tools.performance.embedded.audio [out.wav]` builds+runs the harness and returns the WAV. VERIFIED: the QEMU render produced a valid mono/16-bit/48kHz/16416-frame WAV (peak 8694), and its recomputed rotl5/xor checksum 0ed6e0ac is BIT-IDENTICAL to the counts rig's measured render checksum 0ed6e0ac — so the dump is provably the exact audio the M7 binary renders. Perf gate 7/7 (counts rig unchanged), host syntax-check clean, qemu_main.c untouched.
- Pass 272 — audio.py render_wav now builds in a throwaway temp dir; only the WAV reaches the user's path. The first cut used the WAV's own directory as the harness build dir, so it left 7 build intermediates next to the result (qemu_render_main.elf, the generated spectral_fixture_generated.h, and *.o). render_wav now takes the final wav_path (not out_dir+name), compiles the harness in a tempfile.TemporaryDirectory (or an explicit build_dir for debugging), runs qemu with cwd=that dir, and shutil.move's ONLY qemu_render.wav to wav_path; the build dir is auto-cleaned. Verified: a fresh output dir contains exactly the WAV, still valid (mono/16-bit/48kHz/16416 frames) and bit-identical to the counts render (checksum 0ed6e0ac).
- Pass 273 — the simulate target now forces the real M7 codepath, so a host roundtrip renders the EXACT device oscillator (arch-02 fix). simulation.cmake adds SPECTRAL_ARM_M7=1 SPECTRAL_HAS_DUAL_MAC=1 to the `simulate` target only (same as arm_core_test). Without it the host auto-selects the generic LUT-gather oscillator, so `spectral_*_simulation input.wav` (analyze -> embedded Q15 synth -> output/<input>.wav, the arbitrary-audio roundtrip) rendered a different path than the Daisy/M7 binary. Now it renders the coupled-recurrence oscillator + dual-MAC the device runs; fixed-point is deterministic so the audio is bit-equivalent to QEMU/M7 (only host AoS vs device SoA layout differs, which is not arithmetic). Verified: simulate builds, roundtrips a real 7s file (495437 segments -> 6.9MB WAV, uniquely named after the input), and the M7 render md5 differs from the prior LUT render (7acbca39… vs 9abd7060…) confirming the coupled path is live; simulate_smoke green. NOTE: the QEMU audio tool (audio.py) renders the synthetic perf FIXTURE only (~0.34s) — for arbitrary-audio roundtrip use the simulate binary.
- Pass 274 — embedded plan batch (doc/comment hardening, no codegen on the measured path). cache-04: deleted the redundant dsb at the end of spectral_arm32_init (it ordered nothing — init only does same-core CPU writes to ctx; the load/load_in_place barriers that fence the segment pool are the ones that earn their place); replaced with a comment stating why no barrier is needed. arch-01: documented at the render-dispatch #if SPECTRAL_ARM_M7 that the gate forks the whole OSCILLATOR ALGORITHM (coupled-recurrence vs LUT-gather) = a capability proxied by the CPU macro (a 2nd core extends a capability map, not the CPU id). fpu-07: wrapped the three per-sample RT loops (synth_core_m7 / synth_fade_m7 / synth_core_pair_m7) in // SPECTRAL_Q_DOMAIN BEGIN/END markers so the q_domain_contract scan now ENFORCES no-float-in-the-recurrence on the hot loop (they are verified float-free; after fpu-01 the f64 seed is in the activation, outside the loops). kr-02: corrected the stale DAISY_MAX_ACTIVE comment to the 512-voice target — 128 is the legacy conservative figure (oscillator-bank ceiling measures ~416 @400MHz / ~499 @480MHz at the real 48-block; 512-voice real-time rides the IFFT path for dense, not the per-partial bank); value kept at 128 (perf gate parses it). arch-02 LANDED + the qemu-spq-roundtrip TRACKED-deferred item recorded in the plan. Verified: q_domain_contract OK (9 pure regions), arm_core_test SINAD 80.7, perf gate live PASS (labels unshifted — comments + dsb-removal didn't renumber), all embedded host targets build.
- Pass 275 — kernel-hardening campaign (7 concerns; plan docs/core_audit/active/KERNEL_HARDENING_PLAN.md) consolidated record. **II CLI observability (D-1..D-4):** the headline `Total` was a SUM OF KERNEL TIMERS (under-reported the real run up to ~13×, making `Realtime` a fiction); it is now the real monotonic wall span with a Busy/Idle split + a per-stage "Stage idle:" line (synth idle = backend init/dispatch — Metal ~34ms vs CPU ~13ms). The memory figure was "Peak tracked %.1f MB" — a near-unwired ~3%-of-RSS naming-lie (0.7MB vs 689MB actual); replaced by the real RSS delta + a "Faults: major N ctx-switches N reallocs N" line. A consolidated "Paths:" line records the effective run (req-vs-effective backend, q15, cache, hybrid, proc). perf_track_realloc (atomic) surfaced ~32 no-realloc-contract violations the old per-growth WARN hid. Gate: tests/tools/test_cli_timing.py (Total≥Busy + reaches the independent stage-marker wall; Realtime derived; Memory honest; Paths reflects effective run; per-stage idle present; cache-mode prints Paths). **VI/VII build SSOT:** NEW cmake/profiles.cmake = the flag-group SSOT; x86 ISA flags (-mavx2/-mno-avx512f) are arch-gated (spectral_profile_host_native_isa) → zero -Wunused-command-line-argument (was 88 desktop / 362 tests_all on arm64); included from options.cmake so the Daisy firmware sub-configure gets the groups too; generated-artifact verify_* decoupled from generate_* (drift FATALs on fresh builds instead of being masked). Tests: test_build_flags.py, test_generated_artifacts.py. Canon #26/#28. **§VIII determinism:** root-caused a rare (~1/13) gross arm32 SINAD collapse (~28 dB vs 70 floor, ASan/UBSan-clean) to ThinLTO's non-bit-reproducible parallel codegen × fast-math on the host-compiled fixed-point path — a test-harness fidelity violation (the harness inherited the desktop profile). Fix: arm_core_test compiles -fno-fast-math -fno-lto (firmware-faithful). Canon #27. **V logging:** contract = AI_CANON #29 (stage entry; dispatch decisions + fallbacks; originating SPECTRAL_ERR logs its cause + inputs at the site; existing structured helpers, no third idiom; hot kernels silent; level discipline; embedded uses strippable SPECTRAL_DBG). Backfilled the highest-value silent origins: analyze_audio null-arg + invalid-shape (was a silent empty SegmentArray), FFT-init fail: (was silent return 0), the three spectral_tracker_create NULL exits. IV-d.3: removed the per-growth realloc SPECTRAL_LOG_WARN from inside the OMP parallel emit region (it serialized threads + violated #29; the perf counter + the Faults line replace it). NEW error_origin_logs ctest (#19) drives the real analyze_audio (desktop link set minus main) and asserts the cause strings — verified fail-on-bug. **VII.3:** AI_CANON #30 — SIMD width is widest-available unless that op's latency is worse, decided per-op via the SIMDe natural-width predicates (OSC_SIMD_WIDTH oracle is a 2nd SSOT to retire); AVX-512 off-by-default, lifted per-op only behind a measured win; don't author unmeasurable wider-tier kernels. **IV-d.1 correction:** the −13% magsq-vectorize win is real but its stated gate (full_fused_parity) is INVALID — apply_magsq_scales is shared by both the full and fused paths, so vectorizing it passes that parity trivially while proving nothing about scalar-vs-vectorized output; the double→float 1-ULP shift on the double-scaled peak-scan key can flip a threshold peak, so it is re-classified as a maintainer ULP decision + needs a tolerance'd producer-equivalence test (NOT shipped). DEFERRED: V.3 presence-lint (needs attended allowlist tuning), III embedded telemetry (m7-baseline-gated), VII.2 dispatch migration (x86-CI). ctest 33/33, perf gate + cli timing green across the campaign.
- Pass 275 — arch-05 firmware OS-contract purity (embedded plan). A re-audit (11-finding adversarially-verified workflow) found the lone firmware-purity hole: spectral_wavetable.c (a SPECTRAL_SOURCES_DAISY_ENGINE TU) compiled four host file loaders (load/save/load_raw/load_hex) UNGUARDED into the firmware build — `nm -u` in real-firmware mode named FILE*/sscanf, the spectral_fs_* shim (which is NOT in the firmware link set, so they'd be unresolved externals), and malloc/calloc/free, every one of them inside those four functions (the wavetable bank is fixed arrays; spectral_wavetable_load_buffer is the firmware load path). The other three engine TUs (synth_arm32, debug_embedded_arm, q) were already clean. Fixed by gating the spectral_fs.h include + the four loaders under the emulator guard `#if !SPECTRAL_EMBEDDED || SPECTRAL_IS_EMBEDDED_SIM` — kept on desktop AND the embedded host-sims (which run on an OS and whose CLI exercises file loading), excluded ONLY on real arm-none-eabi firmware (daisy-config.cmake defines SPECTRAL_EMBEDDED=1 with neither SPECTRAL_EMBEDDED_SIMULATION nor SPECTRAL_USE_EMBEDDED_SYNTH, so SPECTRAL_IS_EMBEDDED_SIM=0). NEW tests/tools/test_firmware_purity.py parses SPECTRAL_SOURCES_DAISY_ENGINE, recompiles each TU in real-firmware mode (host cc, the host-sim markers stripped) and asserts `nm -u` (actual CALLS, so <stdio.h>/<stdlib.h> prototypes don't false-trip) names no host file I/O / libc heap / FS-shim / OS-threading symbol. Robust over a static grep (guard-aware) and over the include-DAG layer law (which is structurally blind to host-contract leakage). Fail-on-bug verified: neutering the guard trips the test with the exact ['calloc','free','malloc','spectral_fs_*'×6,'sscanf'] set. All four firmware TUs CLEAN; desktop/simulate/embedded_arm{,_float,_restricted} all link.
