# Core audit pass 119: full/fused parity harness specification

Pass 119 creates the behavioral parity harness specification for Phase F.

It does not pretend a static test is behavioral parity. It documents the forced-path entry points,
deterministic fixtures, comparison tolerances, and required behavior for the compiled harness.
